Ansible playbooks for managing transport.datahub.io
===================================================

To execute the playbook, run the following command:

	ansible-playbook -u USERNAME datahub.yml

Set `USERNAME` to your username at `transport.datahub.io`.
